import type { ApiError } from '@/core/interfaces/ApiError';

/**
 * @deprecated Use ApiError from '@/core/interfaces/ApiError' instead.
 * This type is kept for backwards compatibility.
 */
type ErroDTO = ApiError;

export default ErroDTO;