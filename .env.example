# Variáveis de ambiente — copie para .env.prod ou .env.hml e preencha os valores reais

# Frontend (injetadas no build do Docker)
VITE_API_URL=
VITE_MAPBOX=

# Backend (injetadas no container em runtime)
JWT_SECRET=
SMTP_PASSWORD=
